{
  "name": "ha-spc-flexc-card",
  "version": "1.0.0",
  "private": true,
  "description": "Lovelace dashboard card for the SPC FlexC Home Assistant integration.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/ha-spc-flexc-card.js && node scripts/check-dist.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-or-later"
}
